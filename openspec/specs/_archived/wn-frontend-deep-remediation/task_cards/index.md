# Task Cards: FRONTEND Deep Remediation

任务卡片按优先级分组：

- P0：设计系统基础设施 + Markdown 预览（先打地基）
- P1：布局重构 + 状态栏合并 + AI 上下文绑定（让体验闭环）
- P2：内联 AI + 多标签 + 心流模式（让写作更沉浸）
- P3：语义搜索 + 卡片/看板（未来方向）

每张任务卡必须包含：

- 完成标准（Acceptance Criteria）
- 具体文件变更列表（预期修改/新增）
- 依赖关系（Dependencies）

