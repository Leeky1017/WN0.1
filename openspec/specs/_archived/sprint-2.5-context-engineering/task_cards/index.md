# Task Cards: Sprint 2.5 Context Engineering

任务卡片按优先级分组：

- P0：基础架构（类型/目录/预算/组装器）
- P1：编辑器集成 + Prompt 模板 + 对话历史
- P2：可视化调试 + KV-Cache/性能优化

每张任务卡必须包含：

- 完成标准（Acceptance Criteria）
- 具体文件变更列表（Expected File Changes）
- 依赖关系（Dependencies）
- 预估工作量（Effort Estimate）

