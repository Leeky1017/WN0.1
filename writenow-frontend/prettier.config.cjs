/** @type {import('prettier').Config} */
module.exports = {
  // Why: Keep formatting deterministic across editors/CI without coupling to ESLint rule sets.
  printWidth: 120,
  singleQuote: true,
  trailingComma: 'all',
}

